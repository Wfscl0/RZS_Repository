# RES 文档

- `RES_Controller_接口汇总.md`：当前接口分配、状态指示和接收端约束；
- `Legacy/`：旧产品说明书，仅用于历史对照；
- `Renders/Manual/`：旧说明书关键页面渲染；
- `Renders/Rules/`：现行规则关键页面渲染；
- `Renders/Schematics/`：原理图或参考资料页面渲染。

规则原始 PDF 位于仓库根目录的 `规则/`，本目录不保存重复副本。

