{
  "name": "jlc-bom-netlist-exporter",
  "version": "1.0.2",
  "description": "嘉立创 EDA 专业版 V3 一键导出当前工程的 BOM 和网表",
  "author": "RZS",
  "license": "Apache-2.0",
  "engines": { "node": ">=20.17.0" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "rimraf ./dist && ts-node ./config/esbuild.prod.ts",
    "package": "ts-node ./build/packaged.ts",
    "build": "npm run typecheck && npm run compile && npm run package"
  },
  "devDependencies": {
    "@jlceda/pro-api-types": "^0.3.4",
    "@types/fs-extra": "^11.0.4",
    "esbuild": "^0.24.2",
    "fs-extra": "^11.3.0",
    "ignore": "^7.0.3",
    "jszip": "^3.10.1",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
